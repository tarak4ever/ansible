#- name: ping the server
 # host: frontend # target server private ip
  #tasks:   ##tasks == module == collection ---> all are same "retesting"
  #- name: ping the server
   # ping:
---
- name: ping the server
  hosts: frontend
  tasks:
    - name: ping the server
      ansible.builtin.ping:

